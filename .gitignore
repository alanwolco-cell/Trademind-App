node_modules/
.env
server/cache/
*.log
.vercel
.env*

# local tooling / OS noise
.claude-flow/
.DS_Store
**/.claude-flow/

# local Claude Code settings (per-machine, not shared)
.claude/settings.local.json

# ruflo tooling (symlink a ~/.claude/helpers, no versionar)
.claude/helpers
scripts/.splash-tags.html
